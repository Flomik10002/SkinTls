{
  "schemaVersion": 1,
  "id": "${modid}",
  "version": "${version}",
  "name": "SkinTLS — Force HTTPS for Skins",
  "description": "Forces https:// for textures.minecraft.net skins by rewriting URLs.",
  "authors": [
    "Flomik"
  ],
  "contact": {},
  "license": "All-Rights-Reserved",
  "icon": "assets/skintls/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "org.flomik.skintls.SkinTlsClient"
    ]
  },
  "mixins": [
    "skintls.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
